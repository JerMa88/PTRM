# PTRM - Probabilistic Tiny Recursive Models
# Core dependencies for inference and evaluation

# PyTorch (CUDA 12.4 recommended for GPU inference)
torch>=2.1.0

# Model & data
huggingface_hub>=0.20.0
numpy>=1.24.0
pydantic>=2.0.0

# Configuration
pyyaml>=6.0

# Evaluation & visualization
matplotlib>=3.7.0
scikit-learn>=1.3.0
tqdm>=4.65.0

# TRM submodule dependencies
argdantic>=1.0.0
einops>=0.6.0
hydra-core>=1.3.0
omegaconf>=2.3.0

# Testing
pytest>=7.4.0
